const initialState = {
    errors: false,
    loading: false,
}


export default function foo (state = initialState, action ){
    switch (action.type) {
       
        default: 
            return state
    }
}